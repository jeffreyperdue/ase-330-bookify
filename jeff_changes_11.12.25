x A user switching between chosen shelves on shelf settings now populates with that shelf's current settings
x Improved shelf customization, specifically around shelf color & background (user should have more options for customization)
x A user now has more bookend options beyond simple icons
x + button on my shelf now allows you to either add a new row or to add a book (via routing to home, will change to browse page once created)
x User now be able to change bookend background color
x If a user clicks out of shelf settings but have changed settings, they now are alerted that their changes would be lost
x The carousels on the home page now loop (like Netflix does)
x Added side by side hero on home page of two featured books
x Fixed scroll on my shelf (scroll bar should be hidden)
x Fixed spacing on shelf name in my shelf (move away from edge)
x Added UTUAT survey document that we can use for user testing round 2
